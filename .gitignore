# Run artifacts: the suite writes logs and pipeline build products on every
# run; neither is part of the distributed package or the public record.
*.log
qa/logs/
qa/fixtures/pipeline/output/

# ...but the example logs ARE shipped: an example without the log it produced
# cannot be checked against what it claims. Both depths are needed.
!examples/*.log
!examples/**/logs/*.log

# ...and so is a blessed replay log, for the same reason one step further in:
# it IS the golden master. stqa_replay compares each candidate run against this
# file, so if it is not in the repository there is nothing to compare against
# and the check degrades to "no blessed log found" -- a red that reads like a
# missing feature rather than like drift. Run logs under qa/logs/ stay ignored.
!qa/replay/*.log

# The SSC submission zip is built from src/s/ at release time and mailed, not
# distributed from here. Committing it would leave a copy that goes stale the
# moment src/ changes while still claiming to be what was submitted.
stataqa_ssc.zip
